# ---------------------------------------------------------------------------
# CPS — Default DV Certificate managed by Property Manager
# The property uses cert_provisioning_type = "DEFAULT" which auto-provisions
# a DV certificate. DNS CNAME validation record is in dns.tf.
# The standalone CPS enrollment below was removed in favor of Default DV.
# ---------------------------------------------------------------------------

# Standalone CPS enrollment removed — Default DV handles cert provisioning
# automatically via property hostname configuration.
