apiVersion: v1
kind: Service
metadata:
  name: im-demo
  namespace: demo-im
  labels:
    app: im-demo
spec:
  type: ClusterIP
  selector:
    app: im-demo
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
